package org.sogeti.formation.page;

import lombok.extern.log4j.Log4j2;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
/*
@Log4j2
public class LoginPage {
    WebDriver driver;

    public LoginPage(WebDriver driver) {
        this.driver = driver;
    }

    // Les 3 By
    By userNameId = By.id("user-name");
    By passWordId = By.id("password");
    By loginId = By.id("login-button");


    //Les méthodes
    public LoginPage inputUsername(String username) {
        log.info("input username [{}]" , username);
        WebElement champsLogin = driver.findElement(userNameId);
        champsLogin.sendKeys(

        );
    }

    public LoginPage inputPassword(String password) {
        log.info("input password [{}]" , password);
        WebElement champsPwd = driver.findElement(passWordId);
        champsPwd.sendKeys(password);
    }

    public HomePage login() {
        log.info("input login [{}]" , loginId);
        WebElement loginButton = driver.findElement(loginId);
        loginButton.click();
        return new HomePage(driver);
    }
} */